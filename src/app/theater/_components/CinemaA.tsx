import React from 'react';

interface CinemaAProps {
  name: string;
  description: string;
  price: number;
  show_time: string;
  image_url: string;
}

const CinemaA = () => {
  return <div>야호호</div>;
};

export default CinemaA;
